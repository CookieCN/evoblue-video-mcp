# Security Policy

## 安全边界

- Local Engine 只能绑定 `127.0.0.1`，禁止默认监听 `0.0.0.0`。
- WebUI/API 使用严格 CORS、SameSite Cookie 与本机访问 Token，防止恶意网页调用 localhost。
- URL 仅允许支持平台与 `http/https` 协议；拒绝内网探测、文件协议和重定向逃逸。
- 子进程必须传参数数组且禁止 `shell=True`；输出路径必须落在用户批准的白名单目录。
- 下载前检查视频长度、下载大小、并发、磁盘空间；临时文件与最终报告隔离并可治理。
- API Key 使用系统凭据库，日志与诊断脱敏，Cookie 永不进入日志。
- 遥测默认关闭；删除、覆盖、批量配置等高风险操作需用户确认。

## 报告漏洞

请通过仓库的私密安全报告渠道提交，勿在公开 Issue 中附带密钥、Cookie、私人视频、日志或本机路径。P0 尚未发布安全支持版本。

