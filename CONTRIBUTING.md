# Contributing

1. 先阅读 `AGENTS.md`、`.agents/progress.md` 和相关 `docs/` 合同。
2. 先补合同测试，再实现最小改动；不得让旧 EvoBlue 架构反向污染新边界。
3. 后端运行 Ruff、mypy、pytest；前端运行 lint、test、build。
4. 不提交 API Key、Cookie、Token、真实用户路径或未脱敏日志。
5. Commit message 使用简洁英文；不要由自动化流程执行 `git push`。

功能请求需说明用户问题、边界、验收标准与失败/回滚方式，不提供工期预测。

