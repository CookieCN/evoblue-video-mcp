# EvoBlue Video MCP — Project Handoff

## Purpose

把 YouTube、Bilibili 等视频链接转换为用户拥有的标准 Markdown 知识资产。系统本机提取元数据和字幕，无字幕时可选本地 ASR，再由 LLM 总结、分析、保存和检索。

## Product boundaries

- Local-first：Local Engine 是唯一状态所有者，不建设远程业务服务器、账户、支付、多租户或云同步。
- Markdown 是最终事实资产，SQLite 索引可从 Markdown 重建。
- STDIO MCP Bridge 只调用 `127.0.0.1` Engine，不直接执行下载、ASR、FFmpeg 或数据库逻辑。
- 普通用户通过 WebUI 完成首次设置，不需要手改配置文件。

## Current progress

- P0 已完成：Harness、产品/架构合同、MCP/Job/Markdown/WebUI 合同、前后端骨架和基础 CI。
- P1～P4 已完成：SQLite Job、版本化迁移、状态转换、单 Worker 租约、崩溃恢复、YouTube/Bilibili 字幕分析闭环、Markdown/FTS5/索引重建，以及七工具 STDIO MCP Bridge。
- P4 客户端实测：Claude Code 已完成工具调用；Codex 0.153.4 持久配置已由真实用户态读取并完成 `diagnose_environment` / `list_analysis_jobs`；WorkBuddy 已完成真实握手、七工具发现与提交/状态工具级实测（报告列被 Engine 未完成初始设置阻断，任务按设计停在 queued）；DeepSeek 保持未验证。
- P6/ASR 已完成：Standard SenseVoiceSmall INT8 通过基准并成为唯一正式默认；Lite 未过审、不自动推荐；模型管理、路由恢复、许可与打包门禁均已收口。
- P5、P7、P8 与 ASR-5 均已交付；当前版本为 0.9.0b5，安装器静默卸载提示已改为可抑制且默认保留数据，正在重建发布产物。
- **首位试用者反馈修复进行中（2026-09-10 开工）**：beta.5 真实用户 16 项反馈已立项（`docs/FIRST_USER_FEEDBACK_FIX_PLAN.md`，模块 `fix_first_feedback`）；历史 62/62 done 不变，但发行可用性缺陷未关闭（诊断 401 误报 / 编辑保存失效 / Standard 安装必败 / 字幕未触发，真实用户产不出第一份报告）。F0/F1 已交付：F0 = Engine 滚动文件日志（`docs/ENGINE_LOGGING.md`）+ 证据台账；F1 = LLM 诊断探测带 Bearer 认证（修复 #13 有效 Key 恒 401 误报）、`POST /api/settings/test` 测试连接端点（#8）、DeepSeek 预设 `deepseek-flash` + 旧名迁移提示（#14）、keyring 有界调用与前端保存反馈（#12 防御性修复，根因待发行包复现）、#4 定性符合合同。F2 已交付：Standard/Lite 安装 ARCHIVE_INVALID 根因修复（manifest 白名单补全真实归档全部成员 + 解压器支持嵌套子路径，源码与 frozen 双管线真实归档实证）+ yt-dlp/ASR 运行时诊断误报修复（模块探测 + spec copy_metadata，full/base 实测）。F3 已交付：任务身份（title/platform/url）随元数据阶段回填全链路可见（迁移 v9）；字幕归因三态与 ASR_DISABLED Cookie 提示；queued/waiting_for_model 的 blocked_reason/blocked_message 经 MCP status 透传（真实样本已闭合用户反馈因果链）。F4 已交付：#15 定性为悬挂取消竞态（取消落在 handler 安全点之间、任务停靠 waiting_for_model 时取消悬挂，装模型后复活才落 cancelled；源码穷尽证实重启本身无取消路径），修复三层（advance_job 停靠消费取消 + 启动清扫 + resume 排除取消标记，取消的任务不得复活）；MCP/WebUI 列表 F4 修订（默认含 failed/cancelled 历史 + error_code/url 归因，显式终态过滤单源透传 + 服务端精确 total；WebUI 状态筛选 chips）。F5 已交付：README 直链 Releases 全部版本页 + 测试版产物名（#2）；模型页单一可变形按钮保焦点 + 双击在途禁用 + aria-live 徽标 + 文案对齐 + 后端幂等核查零改动（#10）。评审回合（2026-09-11）：R1-R4 全部修复且反例翻转验证（origin 凭据边界/串行写入+shield/锁内权威取消读/分页下推+有界深取），777 passed 两轮；从零联网下载缺项闭合；F6 回退 in_progress（LLM 闭环、B 站公开 CC 样本两缺项阻塞）。此前：全链路验证 + 双变体构建 + verify_release PASSED + 真机三矩阵（P7 安装 16/16、frozen 综合 34/34、beta.5→修复版原地升级 18/18——v8→v9 迁移/备份/凭据/等待任务保留 + 卡住任务端到端复活）；YouTube found 字幕路径实机闭合；16 项反馈 = 14 修复 + 1 符合合同（#4）+ 1 延期（#6 ZCode 适配，P3 后续）。验收缺项终态（2026-09-12）：LLM→Markdown→FTS 闭环已闭合（真实 Key，CLOSED LOOP 14/14，YouTube 字幕链路 + B 站 ASR 链路双 completed）；从零联网下载已闭合；仅剩 B 站公开 CC 字幕样本（平台匿名门控，需 Cookie 环境或样本链接）。附带修复：AUDIO_DOWNLOAD_FAILED 网络抖动分级。产物在 dist/（SHA256 见 SHA256SUMS.txt）；发布（bump 版本/push tag/审阅 Release）是 Owner 手动动作，未经 Wilson 指示不执行。第二轮评审回合（2026-09-12）：R1b（凭据-端点持久绑定 llm_credential_origin + 原子解绑，迁移 v10，独立于 setup 状态）与 R4b（列表全视图真下推分页：history query 服务端过滤 + jobs status_group 双段分页 + 预算守护，删除 50 页深取上限）修复且反例翻转；闭环归属断言补强（搜索精确 job_id + analysis_id frontmatter 核对）后重建包完整重跑通过；音频重试为有界取舍口径（永久性故障同样耗尽预算后 failed）。全量 785 passed。上轮 R2/R3 无残留。执行以真实发行包两条报告链路为交付标准，未经 Wilson 指示不 push、不发布。
- 详细状态以 `.agents/progress.md` 和 `.agents/feature_list.json` 为准。

## ASR direction

- 平台字幕优先；只有缺少可用字幕时才进入 ASR。
- 基础安装包不携带 ASR 模型。
- 中文模型分两级：约 63.4 MB 的离线 Zipformer CTC small INT8 是 Lite 首体验候选；约 228 MB 的 INT8-only SenseVoiceSmall 是 Standard 候选。两者只有通过对应基准、许可证审查和中国下载验证后才能成为正式推荐。
- 禁止使用同时携带约 894 MB FP32 与约 228 MB INT8 的 SenseVoice 完整制品；生产 Manifest 只能安装实际需要的权重。
- 其他语言由可选 `whisper.cpp` 模型包回退；faster-whisper 仅保留为高级可选能力，不得拖大基础安装包。
- 可选 Qwen3-ASR 0.6B INT8 复用 sherpa-onnx，覆盖 30 种语言与 22 种中文方言；约 941 MiB 文件集从固定 ModelScope 国内提交下载，逐文件校验并支持续传。它尚未通过 EvoBlue 独立基准，不替代 Standard 正式默认。
- ASR 路由不触发隐式下载：缺模型任务持久化为 `waiting_for_model`，WebUI 经用户同意安装后由正常 Worker 恢复。应用设置可固定 Provider；已固定路由写入 Job，升级不会改写既有任务选择。
- `whisper-cpp-base` 钉死 147,951,465-byte ggml Base 制品；模型由 Model Manager 管理，`whisper-cli` 由用户显式配置，基础安装包不携带二进制。
- Model Manager 由 Local Engine 统一负责按需下载、断点续传、SHA-256、安全解压、原子安装、升级回滚和卸载。
- 模型许可按精确制品审查：SenseVoice 使用 FunASR Model License 1.1；Zipformer 转换归档的再分发声明待确认。未经审批的权重或镜像不得进入生产 Manifest。
- Windows 必须在导入 sherpa 前优先注册 ASR 环境的 `onnxruntime/capi` DLL 目录，避免误载 System32 ONNX Runtime 1.10；`version [27] ... only 1 to 10` 是 C API DLL 冲突，不是模型版本。
- 完整执行合同与 ASR-0～ASR-4 验收标准见 `docs/ASR_PLAN.md`。

## Development rules

- 开工顺序：`AGENTS.md` → `.agents/progress.md` → `.agents/feature_list.json` → 相关 `docs/` 合同。
- 先改合同和测试，再实现核心逻辑；修改后运行与风险相称的 test/lint/build。
- 密钥、token、密码不得进入源码、`.env`、日志、快照或 Markdown 报告。
- 不自动 git push，不自动部署，不启动长期运行服务。
- 收工同步 feature list、progress、experience、CHANGELOG 和必要的 AGENTS 架构规则。

## Known environment gotcha

Git 可能因沙箱账户与仓库 Owner 不同报告 dubious ownership，只对单次 Git 命令使用 `git -c safe.directory=...`。如果 Windows sandbox 报 `setup refresh had errors`，先查 `~/.codex/.sandbox/sandbox.YYYY-MM-DD.log`；本项目曾因 `.agents` Owner 被改为 `CodexSandboxOffline` 触发该故障，禁止用长期 Full Access 掩盖问题。
