# Changelog

## [Unreleased]

### Added

- Release 版本升级到 `0.9.0b2` / `0.9.0-beta.2`，用于区分包含 Qwen3-ASR 国内安装选项的新安装包与已发布的 beta.1 产物。

- ASR-5 Qwen3-ASR 可选模型（2026-09-08）：新增 `qwen3-asr-0.6b-int8@2026-03-25` 与 WebUI/设置 Provider 选项，复用 sherpa-onnx 原生 `from_qwen3_asr`；Model Manager 新增多文件 `file-set` 合同与安全安装链（安全嵌套路径、逐文件 SHA-256/大小、集合指纹、Range 续传、原子晋升）。国内源固定到 ModelScope ONNX 导出提交 `9c182309f7bb075f241424441add9e16c5086dfb`，总计 987,023,031 bytes；实测固定 URL 返回 206 Range。Qwen3 只在已安装或用户明确固定时使用，独立门禁前不替换 Standard 正式默认；基础安装包体积不增加模型权重。

- 发布评审第三轮修复（2026-09-08）：可运行不变量改为校验最终状态（`payload.setup_completed ?? current.setup_completed`）——已完成配置上的局部更新（删 Key/切 Provider/清模型）不再能静默保留 true 标志；显式 `setup_completed=false` 为唯一降级出口。回归测试四段（含 DB+keyring 双不变断言）。
- 发布评审第二轮修复（2026-09-08，P1 后端+前端双层闭环）：`setupReady` 要求 Key（新填，或 Provider 未变且已配置）；`PUT /api/settings` 提交 `setup_completed=true` 前按合并后配置 + keyring 实际结果强制可运行不变量（400 拒绝，含空 Key 删除语义缺口），前端绕不过；新增后端 4 + 前端 3 个不变量测试。
- 发布评审修复轮（2026-09-07，4 P1 + 1 P2）：设置页首次配置表单（LLM 四要素 + 报告目录，`setup_completed` 不可再单独提交）；`scripts/check_release_tag.py` npm 归一化 tag 门禁 + 行为测试；release.yml smoke 日志双文件（PowerShell 禁止双流同文件）；卸载钩子无条件清理 `[Code]` 写入的 install-version.txt（静默卸载不再残留安装目录）；env 提供的 `EVOBLUE_LOCAL_TOKEN` 进入浏览器引导 fragment。真机验收 16/16 复跑；699 passed 两轮 + 前端 16 测试全绿。
- P7 + P8 全阶段交付（2026-09-07）：**P7 Windows 安装器与 GitHub Release**——`docs/INSTALLER_RELEASE_CONTRACT.md` 冻结（产物命名/安装布局/单实例/生产化/Bridge 入口/迁移备份/Release 流程）+ ADR 0006；版本统一 0.9.0-beta.1（三处字面量 + `test_version_consistency.py` 互锁）；修复三个发行阻塞：打包引擎裸奔（frozen⇒production）、打包 WebUI 深链接 404（SPA catch-all）、frozen 客户端载荷错误（引擎 exe `bridge` 子命令 + frozen payload 分支）；WebUI token fragment 引导 + apiFetch + 401 TokenGate 门页；单实例 mutex/lock/端口预探测退出码 3/4 冻结中文文案；迁移前 SQLite backup API 快照 `evoblue.db.bak-v<from>`（fail-open）；Inno Setup per-user 安装器（AppId 冻结、自启动默认勾选、卸载默认保留数据）+ 便携 zip + SHA256SUMS；`release.yml` tag→矩阵构建→验证→草稿 Release（Owner 手动发布）。真机验收 **16/16**（静默装/token 门禁/双开退出码 3/frozen bridge 真实 MCP 握手/原地重装/静默卸载保数据）。
- P8 公开测试与 Harness 加固：`docs/HARDENING_MATRIX.md` 14 行失败模式→护栏测试映射；diagnostics 冻结枚举只增不改追加 `worker_runtime`（Worker 认领门禁镜像四态）+ 首页「任务不会开始」横幅（关闭已知问题 TOP1：任务恒 queued 零提示）；`GET /api/diagnostics/export` 脱敏 JSON 附件 + WebUI 导出按钮；故障注入回归（端口占用 exit 4、双开 exit 3、报告写失败脱敏）；`docs/SUPPORT.md` 使用者排解表；RELEASE_VERIFICATION §7/§8/§9（安装器清单 + 人工门 + 审计留档）。

- P5 评审修复轮（2026-09-07，4 P1 + 3 P2）：manual 档 verify 恒 `unverified/manual_tier`（载荷握手不构成配置证据）；CLI 档证据升级为 command+args+scope 字段完整等价（大小写不敏感；env 不可确认即不可验证）；配置证据失效即握手缓存失效（`unverified/config_changed`，status 路径同 reconcile）；restore 先读选中备份再做安全备份（满架剪枝不再吞掉用户选中的恢复点）；JSONDecodeError/非对象条目映射 `CONFIG_UNSUPPORTED`；dotted-key 等价载荷在任何决策前必须可寻址（fail closed）；P5 请求校验失败映射新冻结码 `INVALID_REQUEST`（合同 §7 扩为九码）。`.gitignore` 补 SQLite 旁路文件；ASR 基准可复现断言拆分稳定字段（精确相等）与内存指标（存在性/有限/非负）。真机验收复跑 12/12。全量 **649 passed**（两轮 exit 0）+ mypy strict 99 文件 + ruff + 前端三连全绿。
- P5 全阶段交付（WebUI MCP 自动配置与真实握手，P5-001～006）：**合同冻结**——`docs/CLIENT_CONFIG_WRITE_CONTRACT.md` 十节（五客户端三档注册表 file_auto/cli/manual、路径解析拒绝 mkdir、条目载荷永不写 token、零依赖 TOML 外科合并 + JSON 深合并 + `disabled:false` 归一化、备份哨兵/保留 5/恢复可逆、验证门禁与「已验证/未验证/验证失败」状态字符串冻结、HTTP API 八错误码、字段级提取脱敏、渲染样例字节锁、实测记录）+ ADR 0005（单一写入通道 + pre/post 双重证明 + 自动回滚 + 握手结论只存内存 + CLI 档不碰文件 + 拒绝 mkdir）+ 漂移门禁（合同表 ↔ 注册表/状态标签/错误码/渲染字节逐项锁定）+ 结构门禁（写原语限定 `writes.py`、适配器操作必经写入通道的传递闭包审计、`commit_atomic` 必须 replace 后回读、`remove_atomic` 必须验证消失——反空转断言防模块挪动后门禁空转）。**内核**：`application/handshake.py`（真实 initialize+list_tools、名称与工具集双判据、45s 封顶、stderr 脱敏截断；`scripts/verify_p4_clients.py` 委托且 P4 字节锁原样通过）。**核心**：`client_config/` 的 merge_toml（括号深度状态机、自身子表留区段、tomllib 权威决策、区段外逐字节不变——注释保留由构造保证、pre+post 双证明）、merge_json（BOM/重复键/空文件 fail-closed、只赋一键、键序保持、`disabled:false` 等价不写——真机实测修复）、writes（commit_atomic/remove_atomic 单一写入通道 + 回读 sha256 比对）、backup（同目录兄弟文件、UTC 时间戳+4hex、哨兵表示「原本不存在」、保留 5、恢复先做安全备份）、paths（只认 home+%APPDATA%）。**API**：`/api/mcp-clients` 七端点（列表/状态/安装/验证/移除/备份/恢复 + 可复制配置）加入 P3 信封前缀、`ClientConfigError` 稳定码 404/409/422/503、逐客户端 `asyncio.Lock`（双开页→409）、确认门先于副作用；bootstrap 构造注入（payload=sys.executable、Engine 探针 trust_env=False、探针 httpx 客户端随 lifespan 关闭）。**前端**：`/mcp` 页五客户端卡片——四状态旗（已配置/目标文件/真实握手/Engine 在线）、四操作（安装配置/测试连接/移除配置/恢复备份）、可复制配置区（含 WorkBuddy Trust 提示与手动档专属步骤）、`未验证` 精确文案、确认弹窗携带覆盖警示。**真机验收 12/12**（`scripts/verify_p5_acceptance.py`）：Codex install/verify/remove/restore 全循环 + HTTP 层 18888 隔离实例冒烟；WorkBuddy `disabled:false` 归一化（真机发现后进合同 §3）；Claude Code 真机抓到 `claude mcp get` 无 `--scope` 旗标并修复（大小写不敏感比对一并修正）；Claude Desktop 残留目录真实写入后自清理恢复；DeepSeek manual 档 409。验收后 Codex 保留规范化条目（WebUI 重复安装为真 no-op）、WorkBuddy/Claude Desktop 还原验收前字节。全量 **641 passed**（两轮 exit 0）+ mypy strict 99 文件 + ruff + 前端 lint/test 10/build 全绿。
- P4 全阶段交付（STDIO MCP Bridge 和四客户端，P4-001～007）：**合同冻结**——`docs/MCP_TOOLS.md` 新增 §0.1 信封规则（成功 = `ok:true` 平铺业务字段 / 失败 = `ok:false` + `error{code,message,retryable,detail}`，`isError` 恒 false，信封以 JSON 文本内容交付）、§0.2 十五稳定错误码总表（含 retryable 语义与触发方，`QUEUE_FULL` 标 reserved，`CTranslate2` 检查纠正为 `asr_runtime` 真实栈语义）、§0.3 超时预算表（10/5/10/10/10/15+30/3 秒，机器可解析）、§7 诊断检查名冻结枚举与 list 合并语义；`docs/CLIENT_COMPATIBILITY.md` 冻结 Bridge 启动合同（主形态 `<venv>\Scripts\python.exe -m evoblue_video_mcp.mcp`，不使用 uv run 作主形态——并发客户端争 uv 锁；token 发现顺序只读；`EVOBLUE_LOCAL_TOKEN` 合同别名）与四客户端样例 + 人工验证矩阵；`mcp/schemas.py` 信封模型（`SuccessEnvelope`/`ToolFailure` 真正引用 P0 孤儿 `ToolError`、`stage` 放宽为 ≤32 自由字符串、七工具 `XxxResult` RootModel 判别联合、`CheckName` 冻结枚举）+ `mcp/errors.py`（`STABLE_ERROR_CODES` 冻结元组 + 翻译矩阵）+ `tests/contract/test_mcp_contract_drift.py`（合同 md ↔ 镜像模型 ↔ web `_SECTION_HEADINGS` 三方漂移门禁）+ ADR 0004（信封所有权在 MCP 层、REST 不动显式修订 ADR 0003 话头、list 合并放 Bridge、QUEUE_FULL reserved、不自动拉起 Engine）。**Engine 侧**：`GET /api/diagnostics`（`application/diagnostics.py` 13 项检查按 CHECK_NAMES 冻结顺序并发执行、单项独立超时 4s、任何单项异常只降级自身且 detail 仅含异常类型、路径只回 `redact_path` 脱敏尾部、凭据只报已配置/未配置、网络探测显式 opt-in 且 http 探针可注入、磁盘阈值 1/5 GiB 分级、ASR 运行时经 importlib.metadata 探测不触引擎导入守卫；端点继承 P3 结构化信封并纳入 `_P3_ENVELOPE_PREFIXES`）。**Bridge 侧**：`mcp/engine_client.py`（冻结超时预算常量、token 头、None 参数剔除、非 JSON 体容错、`trust_env=False` 防 loopback 被代理截胡——否则死端口被翻译成 502 掩盖 `ENGINE_NOT_READY`）；`mcp/errors.py` 翻译矩阵（transport: Connect*→`ENGINE_NOT_READY`/Timeout*→`ENGINE_TIMEOUT`，status: 401→`ENGINE_UNAUTHORIZED`+WebUI 重配对指引、legacy `{"detail":码}` 与 P3 `{"error":{code,message}}` 双形状映射、未知形状→`ENGINE_PROTOCOL_ERROR` 只回 `HTTP <status>` 不回包体）；`mcp/server.py` 七工具（`_ensure_strict_arguments` 在注册前收紧 SDK `ArgModelBase` 为 `extra=forbid` 落实「未知输入字段拒绝」——SDK 默认静默丢弃；平铺 `ok` 信封以 JSON 文本内容交付——mcp 2.0.0 强制 outputSchema 顶层 `type:object`，判别联合 schema 不合法，**ADR 0004 预案 A 激活**，RootModel 联合退居运行时校验器、违规载荷降级 `BRIDGE_INTERNAL` 且日志只含工具名；get_analysis_report 历史未命中先探 Job 表区分 `JOB_NOT_FOUND` vs `REPORT_NOT_READY`；list 由 `mcp/listing.py` 合并 `/api/jobs`+`/api/history`——运行中在前、job_id 去重 active 胜出、platform/query 只作用历史源、total 口径合同冻结；`mcp/messages.py` 中文 message 合成；读类工具 readOnlyHint、submit/cancel idempotentHint）；`mcp/__main__.py` stdio 入口（stdout 零日志仅 stderr、`asyncio.run` 单循环托管 server+client 生命周期、token 只读发现 env→`<data_dir>/local_token` 永不生成）+ `evoblue-bridge` console script。**验收**：`tests/integration/test_bridge_stdio.py` 真实握手 5/5（真 Engine 线程 uvicorn + 真 Bridge 子进程 + ClientSession：initialize/七工具注册/平铺信封 JSON 文本/离线 `ENGINE_NOT_READY` retryable/token 文件发现/stderr 日志与 stdout 纯净）；`tests/integration/test_bridge_multi_client.py` 多客户端幂等（两独立 Bridge 进程同 URL 同 job_id + 二次 `reused=true`；并发取消安全不变量——不产生协议错误、至多一方 accepted、终态收敛）；四客户端配置渲染器 `scripts/verify_p4_clients.py`（render 四客户端片段 + check-handshake 真实握手）与合同测试锁样例；**Claude Code 真机端到端验证通过**（`claude mcp add` 后真实会话调用 `list_analysis_jobs` 返回 `{"ok":true,...}`、`diagnose_environment` 13 项检查全真实：FFmpeg 7.1/GPU providers/路径脱敏尾部/llm_api 正确 skipped）；Codex 与 WorkBuddy 配置已渲染待真机接入，DeepSeek 按兼容矩阵标「未验证」。关闭 progress 待做 #2（MCP Envelope 统一）。全量 **563 passed**（两轮 exit 0）+ mypy strict 81 文件 + ruff + 合同漂移测试全绿。

- P3-007 交付 + 三轮终验修复（P3 全部收口）：`storage/rebuild.py` 确定性重扫引擎——单一算法覆盖启动回填/needs_rebuild 自愈/手动触发：排序全目录扫描（跳过点目录与 .tmp、收录冲突副本）、哈希跳过 unchanged、`analysis_id` 归属判定（排序最小路径胜、与库史无关、删库收敛）、absence pass 按**本轮成功认领**判定（损坏/改 ID/重复败者的旧索引行退出搜索）、解析诊断码进 `index_issues`（含 `MD_EMPTY_BODY` warning 生命周期、消失文件批量 resolved）、消失文件标 `missing`（purge 才物理删除且**仅限磁盘确实消失的路径**，新增 `purge_report_document`）、收口 `verify_fts` 漂移即 needs_rebuild；§4 启动对账孤儿化持久化 `running` + 首次回填自动触发 + 进程内互斥（`_scheduled` 同步标志消除触发竞态，背靠背/并发 HTTP 反例）；报告目录权威来源三级解析 settings → **SQLite 外指针文件 `report-root.txt`**（settings PUT 同步写入/清除，自定义目录删库不丢）→ 回退目录，缺失即 fail-safe（`ROOT_UNAVAILABLE`，绝不执行 absence pass；`start_rebuild` 调度前预检，端点 503 可达）；接线 bootstrap lifespan 后台 reconcile（done callback 消费后台异常且日志脱敏为异常类型 + 稳定码；`shutdown()` 优雅等待避免 aiosqlite 线程竞态）与 `POST /api/index/rebuild`（202/409/503 信封）。§7 验收自动化含**生产 lifespan 删库自愈端到端**（测试全程不写 app_settings，断言 `report_directory` 保持 NULL；自定义目录经指针文件恢复）与全部评审反例。根因修复：`_startup_needs_rebuild` 在 session 关闭后复用导致连接泄漏；CRLF 换行（Windows `write_text` 翻译）导致解析器全隔离——合同澄清为「生成侧 LF、读取侧容忍 CRLF」。pyproject `filterwarnings` 将 `SAWarning`/`PytestUnraisableExceptionWarning` 升级为 error 作为资源泄漏门禁。**第三轮终验修复（2 P1 + 2 P2）**：①删库恢复后内容可读——历史详情/报告 section 读取端复用与重建引擎**同一目录解析器**（`resolve_report_root`，settings → 指针 → 回退），生产 lifespan 端到端补 `core_summary` 与 `summary`/`metadata`/`full`/`transcript`(404=解析成功) 读取反例；②root 可用性判定（`root_is_usable`：未配置/不存在/非目录/无法枚举）在**端点预检 + 扫描入口**双重点验证（关闭 TOCTOU 窗口），预检失败先落 `needs_rebuild`/`ROOT_UNAVAILABLE` 再 503，失效自定义指针**不得静默回退**到回退目录（错目录重扫会把原目录全部记录判 missing）——失效指针、TOCTOU、purge 不误删三个反例；③指针文件改崩溃安全原子写（同目录临时文件 + fsync + `os.replace`，失败清理临时文件），故障注入反例证明旧指针逐字节保留、无临时文件残留；④设置双写顺序固定**指针先行**（原子写成功后才提交 SQLite），指针写失败整个保存 503 且数据库与旧指针不动——消除「数据库已采用新目录、恢复指针仍指旧目录」的分裂状态，反例含正 control；⑤`reconcile_on_startup`/`shutdown` 日志统一走脱敏助手（异常类型 + 稳定码，无 `logger.exception` 原始堆栈）。合同 §4 拆分可用性/原子写/双写顺序/读取端共用四条并新增 §7.13/§7.14；HISTORY_SEARCH_API §0/§5 同步。全量 443 测试 + mypy strict 74 文件 + ruff + 合同脚本 27/27 通过。**第四轮终验修复（4 P1 + 2 P2）**：①**扫描中途根失效绝不触发 absence pass**——批处理读失败先探测根（失效即终止整轮，不逐文件隔离），absence pass 前第三次重验根（枚举/读成功≠现在仍可见），`_run` 的 `except RebuildRootUnavailable` 按异常自身 code 落状态；反例×2（`_collect` 后目录消失 / 全部读成功后 absence 前消失，均 purge=true 且索引原样）。②**设置双写=指针先行+串行化+补偿**——进程内 `settings_write_lock` 串行「读当前→写指针→提交 SQLite」，数据库提交失败补偿恢复旧指针后 503（503 绝不留下「新指针+旧数据库」）；反例×2（并发双 PUT 两 200 终态两存储一致 / DB 故障注入后指针回滚+正 control）。③**清空指针走原子墓碑**——`write_report_pointer(None)` 改写空文件（同 tmp+fsync+replace 路径）而非 `unlink`，任何失败向调用方传播并由端点拒绝数据库提交；反例×2（清空失败 503 且旧指针逐字节保留 / 单测 `os.replace` 注入）。④**不可读指针 fail-closed**——新增 `ReportPointerUnavailable`（`RebuildRootUnavailable` 子类，code=`REPORT_POINTER_UNAVAILABLE`）：指针存在但不可读（PermissionError/编码损坏）抛稳定异常，绝不折叠为「无指针」去扫回退目录；读取端同样 503 信封；反例（不可读指针+健康回退目录 → 不扫回退、索引原样）。⑤**启动对账可靠落码**——库空且根不可用直接落 `needs_rebuild`/具体码（不停留误导性 idle），自动重建根失效后外层 catch 保留 `ROOT_UNAVAILABLE`/`REPORT_POINTER_UNAVAILABLE` 不被 `REBUILD_FAILED` 覆盖；反例×2。⑥**诊断 detail 脱敏**——读失败 issue 的 detail 只含异常类型（`unreadable (PermissionError)`），`str(OSError)` 的绝对路径只进本地日志；反例断言存储 detail 与 `/api/index/issues` 响应均无绝对路径。**根因修复（顺带发现）**：WAL 下 deferred 事务「先读后写」在并发写者提交后升级会得到不可重试的 `BUSY_SNAPSHOT`（busy_timeout 不生效）——本轮启动期 park 写首次与 worker 并发即必现 `database is locked`；治本 = 全部「先读后写」事务改 `BEGIN IMMEDIATE`（`db.immediate_write_transaction` helper：worker claim+全部写转换、Pipeline 索引写入、重建批/absence/park/counters、设置保存），写锁毫秒级、长读保持 deferred、清理 fail-safe（取消时不掩盖原始异常）；合同 §6 重写为该机制真相，AGENTS.md Gotcha #5 落档。全量 454 测试（两轮 exit 0）+ lifecycle 20/20 稳定 + mypy strict 74 文件 + ruff + 合同脚本 27/27 通过。**第五轮终验修复（4 P1 + 2 P2）**：①`immediate_write_transaction` 所有权语义修正——成功才 commit（else）、任何异常**含取消**先 rollback 再 raise、finally 只恢复 deferred 选项；旧 `finally: commit()` 会在 body 异常/取消时提交半完成写、并吞掉失败提交；反例×3（body 抛错回滚 / UPDATE 后取消回滚 / 成功提交）。②重建扫描批改**两阶段**——文件读取与解析在**无事务**状态完成（重建绝不跨磁盘 I/O 持写锁），短 IMMEDIATE 事务只做数据库对账；反例：批读取进行中另一连接立即提交成功（旧长持锁会等满 busy timeout）。③治理补漏：取消端点（`request_cancellation` 先读后写）与 ASR 路由 pin（`_provider_options` 读快照后 `pin_job_asr_route` 再读写）改 IMMEDIATE；`storage/repository.py` 全部**自持事务**的写函数入口统一 `_begin_immediate`（幂等自洽，防调用方遗漏；`report_repository` 配对写不自持事务、归调用方管）；取消端点反例可双向证伪——并发写者在读与写之间提交时，guard 在场 200、双层 neuter 后精确死于 BUSY_SNAPSHOT。④设置保存取消安全——指针写与补偿**同步直调**（不进线程池），构成事件循环上不可分割的临界区（取消的线程会迟到覆盖补偿结果）；补偿捕获 `asyncio.CancelledError`（BaseException）覆盖取消；反例×2（save 挂起中取消 / commit 中抛取消，均断言指针恢复旧值+数据库旧值+无残留）。⑤`start_rebuild` 预检的 resolver 异常（`ReportPointerUnavailable`）先按 `exc.code` 落 `needs_rebuild` 再 503，并验证调度标志已释放（二次触发非 409）。合同 INDEX_REBUILD §4 补指针同步临界区、§6 补两阶段结构与治理边界；AGENTS.md Gotcha #5 更新为三层纪律。全量 461 测试（两轮 exit 0）+ lifecycle 8/8 + mypy strict 74 文件 + ruff + 合同脚本 27/27 通过。**第六轮终验修复（2 P1 + 1 P2）**：①**补偿精确化**——`save_app_settings` 增 `commit=False` 模式（不提交、无 commit 后回读），设置端点用它让 commit 成为该事务的**最后一步**：「进入异常处理 ⟺ 数据库未采用新值」，补偿因此精确；旧实现的 post-commit 回读失败/取消会被误判为保存失败而错误恢复旧指针（SQLite=新、pointer=旧）；反例：commit 成功后取消落在 post-commit reconcile → 两存储一致保持新值、补偿未触发。②**guard 覆盖收口 + 结构门禁**——AST 审计补齐漏网的 6 个自持写函数（`claim_job`/`fail_exhausted_retries`/`save_artifact_inline`/`create_download_operation`/`restart_download`/`advance_download_operation`），并新增 `tests/contract/test_repository_transaction_guards.py`：按 AST 枚举 repository 全部自提交写函数、断言每个都调用 `_begin_immediate`——新增写函数自动被门禁抓住，不再靠人工清点。③**`_begin_immediate` 事务状态感知三语义**——调用方 IMMEDIATE 事务内幂等返回（单一所有权单一 commit，修复 `claim_next_job → claim_job` 嵌套）、带未确认 ORM 变更的 deferred **拒绝**（RuntimeError，不静默提交也不丢弃）、只读 deferred 用**空 commit** 关闭读快照（rollback 会 expire 调用方已加载实例 → 曾致 DetachedInstanceError；空 commit 不发布任何工作）；行为反例×3。合同 §6 治理边界、AGENTS Gotcha #5 同步。全量 467 测试（两轮 exit 0）+ lifecycle/engine_recovery 8/8 + mypy strict 74 文件 + ruff + 合同脚本 27/27 通过。**第七轮终验修复（2 P1 + 1 P2）**：①**commit 取消安全**——取消可落在 commit 的 await 上而 SQLite 其实已落盘（实测反例：commit 成功后取消 → 错误补偿制造 SQLite=新/pointer=旧），`immediate_write_transaction` 的 commit 改经**独立任务 + shield** 执行、取消后必须等待确定结果：落盘成功保留新状态再传播取消（补偿反而制造分裂）、落盘失败才向上传播交由补偿；行为反例×2（durable 成功后取消 → 写保留且取消传播 / durable 失败后取消 → 提交错误传播、未提交可补偿）。②**deferred 事务检测换事件层**——`session.new/dirty/deleted` 看不见已 `execute()` 的 Core DML（实测反例：Core UPDATE 后 guard 会静默发布调用方写入、调用方 rollback 无法撤销），`db.build_engine` 挂事务级 DML 追踪事件（BEGIN 清零、cursor 执行 INSERT/UPDATE/DELETE/REPLACE/DDL 时置位），`ensure_immediate_transaction` 三语义：IMMEDIATE 内幂等返回 / 检测到 DML 或 pending ORM 变更（含 `deleted`）的 deferred **拒绝** / 只读 deferred **空 commit** 关闭（rollback 会 expire 调用方已加载实例）；`immediate_write_transaction` 与 repository `_begin_immediate` 统一委托该语义。③**AST 门禁修复假阳性**——守卫必须是函数体**首语句**（AST 调用节点+顺序，先于任何读写，杜绝「某处调用」与 docstring 文本误报），删除按文本找 `rollback` 的断言（`ast.unparse` 会把 docstring 单词算进去）；guard 语义改为行为测试（Core DML 拒绝且调用方 rollback 可撤销、ORM pending delete 拒绝、幂等、只读关闭）。合同 §6 治理边界与 AGENTS Gotcha #5 同步。全量 471 测试（两轮 exit 0）+ lifecycle/engine_recovery 8/8 + mypy strict 74 文件 + ruff + 合同脚本 27/27 通过。**第八轮终验修复（2 P1）**：①**verdict 等待抗重复取消**——第二次 `task.cancel()` 会穿透未 shield 的 verdict 等待、直接取消 commit task（实测：commit 已落盘、上层收到 CancelledError、端点错误补偿 → SQLite=新/pointer=旧分裂）；修复 = verdict 等待改为**循环 shield**（每次外部取消都落在 shield 上、verdict 永不被取消），直到 commit task 真正完成才裁决——落盘成功保留写并传播取消，失败才传播提交错误交补偿；反例：durable 成功后连续两次取消 → commit 协程自身从未被取消、写保留且取消传播。②**写检测弃用 SQL 前缀、改用 SQLite authorizer**——`startswith(INSERT/UPDATE/DELETE)` 漏掉注释前缀 UPDATE 与 `WITH...UPDATE`（实测两者均绕过检测后被「只读空 commit」发布、调用方 rollback 无法撤销），且触发器内部写入语句文本根本不提；改为连接级 **authorizer**（prepare 时数据库级报告写动作，`SQLITE_INSERT/UPDATE/DELETE` 经 `driver_connection.set_authorizer` 注册，SQLITE_OK 只观察不拦截），标记存池记录 info（与 BEGIN 清零、guard 读取同一 dict）；反例×3（注释前缀、CTE UPDATE、触发器内部 INSERT 均拒绝且 rollback 后零发布）+ 纯 SELECT 事务不被误拒的正对照。合同 §6 治理边界与 AGENTS Gotcha #5 同步。全量 475 测试（两轮 exit 0）+ lifecycle/engine_recovery 6/6 + mypy strict 74 文件 + ruff + 合同脚本 27/27 通过。**第九轮终验修复（1 P1）**：**prepared-statement 缓存致 authorizer 失明**——authorizer 只在语句 prepare 时触发，而 sqlite3 模块默认按连接缓存 prepared statement：同一连接第二次执行相同 SQL 字符串直接复用缓存、不再 prepare、authorizer 不触发（实测：同 SQL 连续三轮事务，第一轮 refused、第二三轮 MISSED，guard 误判只读后空 commit 把写入发布，final_value=99；参数化 Core/ORM DML 共享同一 SQL 字符串，池化连接上是常态而非特例）；修复 = `build_engine` 连接参数 `cached_statements=0` 禁用语句缓存，每条语句重新 prepare、每个写动作都被观测（本应用写路径低频，重复 prepare 开销可忽略；aiosqlite 的 `**kwargs` 直通 `sqlite3.connect`）。同类相邻洞一并关闭：**DDL 动作（建/删/改表、索引、视图、触发器、虚表）入写动作集**——deferred 会话里的 DDL 同样是调用方写，空 commit 发布它一样是静默泄漏（迁移走 engine 级事务不经守卫，不受影响）。反例×2：同 SQL 三轮连续事务全部 refused 且终值保持原值（探针修复前后对照：`['refused','MISSED','MISSED'], 99` → `['refused','refused','refused'], 0`）、deferred 内 CREATE TABLE 被拒绝且 rollback 后零发布。合同 §6 与 AGENTS Gotcha #5 同步（cached_statements=0 为 authorizer 方案的存在前提）。全量 477 测试（两轮 exit 0）+ lifecycle/engine_recovery 7/7 + mypy strict 74 文件 + ruff + 合同脚本 27/27 通过。**第十轮终验修复（1 P2）**：**可写 PRAGMA 绕过写检测**——`PRAGMA user_version = 123` 的 authorizer 动作码是 `SQLITE_PRAGMA`，不在 DML/DDL 动作集内（实测：guard 误判只读 → 空 commit 发布 → rollback 无法撤销，`{'before': 0, 'guard': 'MISSED', 'after_rollback': 123}`）；修复 = 写动作集从「DML + DDL」扩为**所有可能改变数据库或连接持久状态的动作**（全动作码审计后新增 `SQLITE_PRAGMA`、`SQLITE_REINDEX`、`SQLITE_ANALYZE`、`SQLITE_ATTACH`、`SQLITE_DETACH`；读 PRAGMA 报同名动作码、一并计入——fail-closed：拒绝一个只读事务代价微小，静默发布未持有的写不可接受；明确排除纯读 READ/SELECT/RECURSIVE、函数调用 FUNCTION（bm25/snippet 属搜索读路径）、事务控制 TRANSACTION/SAVEPOINT（不写数据，SAVEPOINT 为配对写机制载体）；connect 钩子的 WAL PRAGMA 在 authorizer 注册前执行且任何 BEGIN 都会重置标记，不受影响）。反例：写 `user_version` → guard 拒绝 → rollback 后版本保持 0（探针修复前后对照：`MISSED/123` → `refused/0`）。合同 §6 与 AGENTS Gotcha #5 措辞同步为「所有可能改变数据库或连接持久状态的动作」。全量 478 测试（两轮 exit 0）+ lifecycle/engine_recovery 7/7 + mypy strict 74 文件 + ruff + 合同脚本 27/27 通过。**第十一轮终验修复（1 P2，测试基础设施）**：**新事务测试泄漏 AsyncEngine**——`_make_factory` 只返回 session factory 与路径、engine 所有权丢失，17 个测试各泄一个 engine，全量运行实际产生 `PytestUnhandledThreadExceptionWarning: RuntimeError: Event loop is closed`（aiosqlite 工作线程在 loop 关闭后回调，时序相关故单文件重跑不可复现）；修复 = helper 改 **yield fixture 托管 engine**（teardown `await engine.dispose()`），17 个测试签名/调用点注入改造；`pyproject` filterwarnings 增 `error::pytest.PytestUnhandledThreadExceptionWarning`（与既有 SAWarning/PytestUnraisableExceptionWarning 同为资源泄漏门禁——exit 0 不再能隐藏时序性泄漏）。全库泄漏面审计：migration_upgrade（逐 engine dispose）、index_rebuild（`_Env.dispose` + fixture）、worker_lease（try/finally）、runtime_lifecycle（逐段 dispose）、integration conftest（yield fixture）均已在场，唯一泄漏源即本文件。全量 **478 passed 零 warning**（摘要行无 warnings 后缀，两轮）+ lifecycle/engine_recovery 7/7 + mypy strict 74 文件 + ruff + 合同脚本 27/27 通过。
- P3-006 交付 + 终验修复轮（索引接线 + 历史/搜索 API）：`reports/parser.py` 冻结 Schema v1 解析器（严格 frontmatter 逐行解析 + 精确二级标题切分；BOM/未知版本/缺失必填/naive 时间/off-enum/正文锚点缺失（`MD_BODY_STRUCTURE_INVALID`）/非 HTTP(S) URL/非小写 platform 均抛稳定诊断码；容忍裸 isoformat 与 JSON 引号两种冻结 datetime 形态，renderer→parser round-trip 单测）；`IndexingHandler` 接线（读回文件哈希校验 → 解析 → SAVEPOINT 配对 upsert → handler 提交），pipeline 集成测试断言索引行与 verify_fts 一致；repository 增 `list_report_documents` 与 `search_reports`（bm25 排序、六列 snippet 与 MATCH 同语句计算并取第一个实际命中列、per-column matched_fields）；web 新增 `/api/history`、`/api/history/{id}`、`/api/history/{id}/report?section=`（七 section 含冻结标题、5 万字符截断）、`/api/search`（FTS 故障映射 503 `SEARCH_INDEX_UNAVAILABLE`；snippet 经展示反归一化输出自然中文 `[选品]` 而非存储形态 `[选 品]`）、`/api/index/status`、`/api/index/issues`，P3 端点统一 `{"error":{code,message}}` 信封（含 RequestValidationError 按 P3 前缀分流，旧端点保持默认形状），文件读取带路径逃逸守卫。全量 417 测试 + mypy strict + ruff + 合同脚本 27/27 通过。
- P3-005 持久层交付 + 两轮终验修复：迁移 v8（`SCHEMA_VERSION=8`，`_V8_STATEMENTS` 10 条 DDL 逐字符转录自 `docs/FTS5_SCHEMA.md` §2，合同漂移检测测试 `tests/contract/test_p3_v8_ddl_drift.py` 守护）；`storage/fts.py` 冻结文本算法（`normalize_for_fts`/`build_match_query`，索引与查询同构）；`storage/report_repository.py`（SAVEPOINT 保护的配对原子写入、更新复用 `report_documents.id`、stale 保留/missing 删除 FTS 行且仅允许 `active → stale`、open issue「更新→解决→复发新建」协议、`verify_fts` 严格 active+stale 口径、`FtsBodyTexts` 只收 summary/transcript 其余 FTS 列从 entry 唯一派生、`ReportIndexEntry` 集中校验冻结不变量含 summary_mode 三值必填枚举（无空串，v8 新表无 legacy 兼容需求）/HTTP(S) URL/platform 小写/`math.isfinite` 时间戳；函数不提交、调用方持有事务）。`storage/db.py` 严格按 SQLite savepoint 官方 recipe（connect 事件 `isolation_level = None` + begin 事件显式 `BEGIN` + connect 事件 WAL PRAGMA `await_` 桥），修复 sqlite3 驱动 legacy 事务把最外层 RELEASE SAVEPOINT 变 COMMIT 的驱动级缺陷；`BEGIN IMMEDIATE` 走受控 execution option `db.BEGIN_IMMEDIATE_OPTION`（begin 钩子读取后发送），`application/submit.py` 删除手写 COMMIT/BEGIN 并新增「外部持锁 → busy 超时失败 → 同一 Session 复用成功且无半创建 Job」反例。`init_db` 增 `target_version`（仅供测试构造历史库）。门禁测试覆盖：新库直迁 v8、v7→v8 升级保数据且与全新安装收敛、迁移幂等、更新无 rowid 漂移无孤儿、savepoint 下「异常捕获后外层 commit」无半状态、三态完整性、issue 闭环、入参不变量负向测试；全量 pytest 385 + mypy strict + ruff + 合同脚本 27/27 通过。
- ASR-4 基准门禁与默认模型审批：`asr/release_gate.py` 冻结阈值 v1（按 tier 语言范围裁剪、零样本 fail-closed、静音幻觉率门禁）+ `asr/benchmark` 扩展（real sherpa tiers 经生产注册链路、CER/WER 双归一化、实体召回、RTF、峰值内存、`--gate` 退出码）+ `scripts/build_benchmark_corpus.py` TTS 语料 v1（SAPI 原生合成 + numpy 重采样/混噪/混乐，语料不入库、逐文件钉 SHA）。真实执行：Standard 全项 PASS 审批为正式默认；Lite 实体召回 0.50 未过审。`asr/approvals.py` 精确 `(model_id, version)` 审批注册表接入 ModelManagerService/路由 handlers/WebUI 标签；证据 `docs/ASR_RELEASE_GATE.md` + `benchmarks/results/`。
- ASR-4 打包矩阵与发布验证：生产入口 `evoblue_video_mcp/__main__.py`（loopback uvicorn、production token 首启生成并持久化到数据目录、WebUI 静态挂载且 API 优先）；PyInstaller onedir 双变体 spec + `scripts/build_package.py`（base 47.4 MB / full 120.9 MB，ASR 运行时增量 73.5 MB 实测，模型权重永不入包）；CI `package` workflow（Windows base+full / macOS / Linux，仅产物上传）；`scripts/verify_release.py` 八项打包链路验证（干净档案、审批标志、token 401/200、损坏模型隔离）全过；`docs/RELEASE_VERIFICATION.md` 人工清单（含中国大陆网络 profile）与 `docs/MIGRATION_ROLLBACK.md`（v1–v7 + 备份回滚演练）；`THIRD_PARTY_NOTICES.md` 升级为锁定依赖清单并随包分发。

- P3 四合同冻结 + 评审修复轮（实现待开工）：`docs/MARKDOWN_SCHEMA.md` 升级为 v1.0 冻结版（frontmatter 字段表 + 段落解析锚点 + `analysis_id ≡ job_id` + tz-aware datetime 强制）；新增 `docs/HISTORY_SEARCH_API.md`（历史/搜索/索引管理端点 + `GET /api/index/issues` 诊断明细 + 结构化错误信封 + P4 MCP 映射）；新增 `docs/FTS5_SCHEMA.md`（迁移 v8 逐字符 DDL、自带文本 FTS5 表、CJK 查询合同：空白分词 + 词组短语邻接 + 引号双写转义）；新增 `docs/INDEX_REBUILD.md`（唯一重扫算法、隔离诊断码、幂等崩溃恢复、重复 ID 收敛规则、12 条验收标准）；`docs/adr/0003` 记录决策。评审修复 6 P1 + 2 P2（共 8 项；查询合同修复合并覆盖短语邻接与引号转义两条）：短语邻接反例、引号转义、`uq_index_issues_open` 部分唯一索引、stale 三合同统一、启动孤儿化 running（去心跳）、重复 analysis_id 胜者与库史无关、`GET /api/index/issues` 端点、datetime 时区强制（`utcoffset()` 判定 + 负向单测）。`scripts/verify_p3_contract.py` 从文档原文提取 DDL 执行，27 项检查（含全部评审反例）全过。
- Silero VAD 改为受管依赖随基础包分发（`asr/assets/` + `asr/vad.py` 钉 SHA 校验）：识别归档不含 VAD，注册前 fail-closed 校验，缺失/损坏时阻止 Provider 注册而不是转写中途失败；`docs/ASR_MODEL_LICENSES.md` 补 VAD 钉 SHA 与打包决策。
- Silero VAD 完整 MIT 许可文本随包分发（`asr/assets/silero_vad.LICENSE`）并全文录入 `THIRD_PARTY_NOTICES.md`；修正 `docs/ASR_MODEL_LICENSES.md` 中「基础安装包不包含任何模型权重」的旧表述（现为仅随包附带 VAD）。
- ASR-3 release hardening：统一 `reconcile_waiting_asr_jobs()`（双门禁：SQLite 安装记录 + Provider 注册），接入 Engine 启动、安装完成与保存 whisper CLI 路径三个恢复触发点，消除安装提交后崩溃与 CLI 后配置导致的 `waiting_for_model` 永久等待；文件门禁 Provider 注册抽至 `asr/registration.py`。
- `PUT /api/settings` 的 `asr_provider` 收紧为 `auto`/`sherpa-onnx-lite`/`sherpa-onnx-standard`/`whisper-cpp-base` Literal 枚举，非法值 422 拒绝。

### Changed

- P4 Codex 持久配置验收收口：真实用户态 Codex 0.153.4 的 `codex mcp list` / `codex mcp get evoblue-video` 读取 `~/.codex/config.toml` 成功；无 EvoBlue 临时覆盖的只读 ephemeral 会话真实调用 `diagnose_environment` 与 `list_analysis_jobs(limit=1)` 均返回 `ok:true`。记录 Codex 沙箱使用隔离 `C:\Users\CodexSandboxOffline\.codex` 的边界，避免把沙箱内“零服务器”误判为用户配置失败；兼容矩阵、README、进度与 Agent 上下文同步为同一事实口径。
- `asr/registration.py` 改为期望状态对账：CLI 更换/清空、模型文件删除时替换或注销对应注册（仅限本模块注册的条目，外部注册不受影响），`ModelManagerService.uninstall` 即时注销对应引擎。
- Provider 加载异常隔离：构建失败只让该 Tier 保持未注册（记录脱敏错误 + 已知坏签名避免高频轮询重复重载），安装完成/设置保存事件路径显式重试，绝不阻塞 Engine 启动；`_MANAGED` 记录 owned instance，注销前校验注册表对象归属，卸载统一走 `unregister_managed_provider()`。
- VAD 资源访问全部包进故障边界：资源缺失/不可读/materialize 失败记稳定错误码（`ASR_VAD_ASSET_MISSING` / `ASR_VAD_ASSET_HASH_MISMATCH` / `ASR_VAD_ASSET_UNAVAILABLE`）并返回 None，损坏安装降级为 Tier 未就绪而非 Engine 不可启动；Provider 加载失败日志脱敏为 provider_id + `ASR_PROVIDER_LOAD_FAILED` + 异常类型。

- 统一 Python 包名 `evoblue_video_hub` → `evoblue_video_mcp`，对齐项目名 `evoblue-video-mcp`。
- 新增 SQLite Job 模型（`storage/models.py`）、版本化迁移（`schema_migrations` + `SCHEMA_VERSION`）。
- 新增状态机转换规则（`jobs/transitions.py`），非法转换抛 `TransitionError`。
- 新增单 Worker 租约闭环（`storage/repository.py`）：原子 compare-and-swap 领取、乐观锁、租约过期接管与恢复。
- 新增覆盖状态转换、并发领取、崩溃恢复、终止态保护与迁移幂等的测试。
- 新增 Worker 执行循环骨架（`runtime/worker.py`）：`StageOutcome`/`StageHandler` 协议与 `run_worker_once` 连续推进。
- 新增 Engine 启动恢复（`runtime/engine.py`）：`recover_on_startup` 释放过期租约并失败耗尽重试。
- 新增 `docs/ASR_PLAN.md` 与 Harness 交接入口，定义 ASR Provider、模型 Manifest、SenseVoice 验证、Model Manager、中国下载链路及发布门禁。
- 新增项目交接文件 `Codex.me`，并将其纳入 `init.sh` 必备文件检查与开工读取顺序。
- 新增 AppSettings 模型与迁移 v2（精确 per-table 迁移），`list_jobs` 与设置 get/save repository。
- 新增 WebUI 后端 API（`/api/jobs` 列表/详情、`/api/settings` GET/PUT）与前端路由骨架（任务列表 + 首次设置 + Vite loopback 代理）。
- 新增平台字幕获取（`platforms/`）：URL 检测、`PlatformAdapter` 协议、yt-dlp 适配器（元数据 + 字幕）、VTT/SRT 解析器。
- 完成 P2 字幕分析闭环：字幕清洗、确定性分块、LLM Provider 合同（OpenAI 兼容 + Fake）、结构化综合、Markdown 渲染与原子写入（不可覆盖 + 内容寻址 + 冲突副本）。
- 组装生产 runtime：提交去重 + 取消 API、handler factory、worker 生命周期、迁移 v4、凭据进 keyring、配置指纹含 base_url + 版本。
- 加固：实时租约续期、取消安全点、chunk summary checkpoint、报告崩溃恢复幂等、worker 异常边界脱敏。
- 新增 ASR Provider 合同层（`asr/`，ASR-0）：`ASRProvider` 协议、归一化 `ASRResult`/`ASRRequest`/`ASRCapabilities`、稳定错误码。
- 新增模型 Manifest Schema（`asr/manifest.py`）：校验 sha256、路径安全 slug、已知平台，`load_manifest` 全量校验。
- 新增确定性 `FakeASRProvider` 与基准脚手架（`asr/benchmark/`）：语料 Manifest、CER/WER/边界误差评分、`python -m evoblue_video_mcp.asr.benchmark` 可复现命令。
- 新增 ADR 0002（可插拔 ASR 与带外模型交付）与 `docs/ASR_BENCHMARK.md`。
- 新增 ASR-1 sherpa-onnx Provider、Lite/Standard 注册、真实引擎集成测试、分段 checkpoint/resume 接线与 Windows ONNX Runtime 显式依赖。
- 完成 ASR-1 验收：60 分钟合成输入分段有界单调、运行中取消 + checkpoint/resume 真实验证、Lite/Standard + Silero VAD 许可证清单（`docs/ASR_MODEL_LICENSES.md`）。
- 新增 ASR-2 第一步合同：Manifest `redistribution` 三态（upstream_only/mirror_approved/blocked）+ release gate；`model_install_state` 表（迁移 v5）与下载状态读写。
- 加固 ASR-2 合同（ASR-2.1）：Manifest 全不可变 + `redistribution` 必填 + 来源审批 + 文件 allowlist；拆 `model_install`/`active_model`/`model_download` 三表 + 下载状态机 + revision 乐观锁。
- 补全 ASR-2 合同（ASR-2.2）：迁移 v6（保留冻结 v5）+ 受信任 Catalog 制品级审批（fail closed）+ 制品一致性（SHA 一致/sum(files)/archive_format/重复文件）+ `update_download_progress` 进度 CAS + `activate_installation` 原子激活。
- 实现 ASR-2 下载器/安装器：Catalog 钉制品 SHA/size、verifying 要求下载完整、同一模型单非终态操作 DB 约束、`raw` 制品格式；`model_manager.py` 提供 HTTP Range 续传 + 校验、bounded staging 解压 + 文件 allowlist + 原子晋升。
- 完成 ASR-2 第三步（WebUI 模型管理 + 中国源资格验证）：`asr/manifests.py` 内置 Lite/Standard 生产 Manifest（钉入实测逐文件 SHA）、`asr/service.py` 模型管理服务（后台安装/取消/卸载 + `ModelSummary`）、`/api/models` 端点（list/install/cancel/uninstall）、前端 `/models` 页（体积先行同意、进度、取消、卸载）。
- 实现 Manifest 多源回退：`installer` 按 `sources` 顺序下载，某源失败自动切换下一源（切源即从零下载），`repository.switch_download_source` CAS 更新；Lite Zipformer 进入 `APPROVED_CATALOG`（`upstream_only`）。
- 新增 `docs/ASR_CHINA_SOURCE_QUALIFICATION.md` 留证（hf-mirror 实测可达 + 支持 Range + 散文件体积与钉死值一致，但「暂不落镜像」）与 `scripts/qualify_china_sources.py` 只读验证脚本、`scripts/measure_manifest.py` 制品测量脚本。
- 完成 ASR-3：新增语言感知 Lite/Standard/Whisper 路由、应用级 Provider 固定、持久化 `waiting_for_model` + 单模型安装建议、安装成功自动恢复等待任务，且全程禁止隐式下载。
- 新增可选 `WhisperCppProvider`（无 `shell=True` 的 CLI JSON 适配器）与钉死的 `whisper-cpp-base` raw Manifest；模型与 CLI 均不进入基础安装包。
- Markdown Schema v1 增加 `asr_provider`、`asr_model`、`asr_model_version` 可选身份字段，生成报告记录实际识别链路。

### Changed

- 后端验证从 `evoblue_video_hub` 迁移到 `evoblue_video_mcp` 包路径。
- P6 从单一 Faster-Whisper 方案调整为可插拔本地 ASR：SenseVoiceSmall INT8 为中文优先候选，whisper.cpp/faster-whisper 为按需兼容能力；基础安装包不携带模型权重。
- P6 中文 ASR 调整为 Lite/Standard 两级：约 63.4 MB 的离线 Zipformer CTC small INT8 作为 Lite 候选，约 228 MB 的 INT8-only SenseVoiceSmall 作为 Standard 候选；禁止生产 Manifest 下载包含约 894 MB FP32 权重的 SenseVoice 完整包。
- ASR-1 验收后转入 ASR-2；Model Manager 按“合同与状态 → 安全下载/安装 → WebUI → 中国源资格验证”顺序交付，并增加制品级再分发审批门禁。
- 修正模型许可证口径：SenseVoice 精确归档适用 FunASR Model License 1.1；Zipformer 精确归档未携带许可证文件，镜像再分发保持待审批。
- 模型再分发口径落定（ASR-2 第三步）：SenseVoice 与 Lite 均保持 `upstream_only`，暂不 ship 任何 `mirror_approved` 源；多源回退机制已就位，待中国源获批后「中国源优先 + 上游回退」无需改安装器。
- ASR-3 自动建议与正式默认批准分离：当前无模型中文建议 Standard、已装 Lite 可完成中文单包体验；Lite 许可证未确认且 ASR-4 基准未完成，因此所有 Tier 的 `formal_default` 仍为 false。

### Fixed

- 修复租约易主后旧 Worker 仍能推进状态的问题：`advance_job` 现校验 `lease_owner` + 租约有效性，失败抛 `LeaseLostError`。
- 修复 `max_attempts` 不生效的问题：领取与查询均约束 `attempt < max_attempts`，耗尽原子转 `failed`（`MAX_ATTEMPTS_EXCEEDED`）。
- 修复恢复扫描「先读后写」竞态：`recover_stale_jobs` 改为带条件的原子 UPDATE。
- 修复 Repository 返回依赖 `expire_on_commit=False` 的问题：写后重新读取，默认 Session 下不再 `MissingGreenlet`。
- 修复 ASR-0 import 守卫扫描范围过宽：改为按架构边界限定（豁免 `asr/providers/`），并新增基础依赖不含引擎库的硬约束守卫。
- 修复 ASR 归一化结果无约束：`ASRSegment`/`ASRResult` 改为 Pydantic，强制非负有限时间、`end >= start`、单调顺序与身份必填。
- 修复 `segment_boundary_error` 除以段数导致放大两倍且静默忽略缺失/幻觉段：改为除以边界数并对未匹配段加惩罚。
- 修复 ASR 归一化结果构造后仍可被改回非法状态：`ASRSegment`/`ASRResult` 冻结（`frozen=True`）并用 tuple 承载 `segments`/`warnings`。
- 修复静音样本幻觉被评满分：CER/WER/边界误差在空 reference 且 hypothesis 非空时返回 1.0。
- 修复 Fake 无法表达零片段结果：改用 `segments is None` 判断，零片段完成时进度报 1.0。
- 修复 import 守卫对动态 import 的能力被过度声称：新增字面量 `importlib.import_module` 扫描，并明确严格隔离依赖 Provider 注册表。
- 修复 Windows 错载 System32 ONNX Runtime 1.10 导致 sherpa C API version 27/10 冲突：导入 sherpa 前固定 ASR 环境 DLL 目录。
- 修复真实 ASR 被 Fake 测试掩盖的四处缺陷：VAD 未 flush、把 `front` 当函数、sample offset 未换算为秒、Recognizer 未调用 `decode_stream()`；音频改为 512-sample 流式 VAD，避免长文件整段常驻内存。
- 修复模型 helper 漏保留 Lite `bbpe.model`，并用测试确保不提取未使用的 FP32 `model.onnx`。
- 修复多源回退「同 SHA ⇒ partial 跨源可复用」的错误推论（P1）：坏源留下的错误前缀会被跨源续传拼接成完整坏文件导致持续失败；现切源即清空 partial + 归零字节从零下载，且 `DigestMismatchError` 也触发回退（不再对坏 primary 直接 failed）。
- 修复卸载一致性（P1，两轮复核）：原子改名到 `.trash/<model_id>/<unique>` → 提交 DB → 删 trash；DB 删除失败时立即恢复原位；启动 `cleanup_trash` 按 DB 记录判断恢复或删除，消除「改名后、DB 删除前崩溃」的孤儿窗口；API 用 `reclaimed_bytes`（实际回收）+ `pending_reclaim_bytes`（待系统清理）区分，前端不再把 pending 数字谎报为「已释放」。
- 修复 `ModelManagerService.close()` 无条件关闭 HTTP client（P2）：仅在服务自建 client（`_owns_client`）时关闭。
- 修复后台安装任务异常静默丢失（P2）：捕获并记录 DB/系统异常，避免 `Task exception was never retrieved`。
