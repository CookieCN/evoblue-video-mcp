使用 project-init Skill 初始化这个新项目，并完成 Phase 0：Harness、开发文档、架构合同和最小代码骨架。

项目名称：EvoBlue Video MCP
仓库名称：evoblue-video-mcp
项目规模：长期维护的开源项目
默认许可证：Apache-2.0
当前日期：使用系统当前日期

# 一、项目目的

这是一个本地优先、开源的视频理解与知识管理工具。

用户在本地 WebUI 或支持 MCP 的 AI 客户端中提交 YouTube、Bilibili 等视频链接，系统在用户电脑上完成：

视频元数据获取
→ 字幕提取
→ 无字幕时可选 Faster-Whisper 本地 ASR
→ 字幕清洗
→ 分块
→ LLM 总结
→ 生成标准 Markdown
→ 保存历史记录
→ 在本地 WebUI 中搜索、查看和管理。

需要支持的 MCP 客户端：

- Codex
- Claude Desktop
- DeepSeek Harness
- WorkBuddy

# 二、核心产品原则

1. Local-first

- 不部署远程业务服务器。
- Local Engine 只监听 127.0.0.1。
- 视频任务、字幕、报告、配置和历史默认保存在本机。
- LLM 和视频平台访问仍然需要网络。
- 遥测默认关闭。

2. WebUI 是控制中心

所有初始配置只能通过 WebUI 完成，包括：

- LLM Provider、Base URL、Model 和 API Key
- 报告目录
- Markdown 文件模板
- ASR 模型和计算设备
- Cookie 浏览器
- MCP 客户端检测、安装和验证
- 存储、隐私和日志设置

不要要求普通用户手动编辑 JSON、TOML、YAML 或环境变量。

3. MCP 是调用入口，不拥有业务状态

- MCP Bridge 是薄适配器。
- MCP Bridge 不直接执行视频 Pipeline。
- MCP Bridge 连接本地 Local Engine。
- Local Engine 拥有 SQLite、Job Worker、Pipeline、WebUI 和报告。
- 关闭 Codex、Claude Desktop、WorkBuddy 或 DeepSeek Harness 后，Local Engine 中已经提交的任务应继续执行。
- 多个客户端调用时不能重复执行同一任务。

4. 长任务必须异步化

Codex、DeepSeek Harness 等 MCP 客户端可能对工具调用设置约 60 秒超时。

禁止设计长时间阻塞的 analyze_video 工具。

采用：

submit_video_analysis
→ 立即返回 job_id
→ Local Engine 后台处理
→ get_analysis_status
→ get_analysis_report

5. Markdown 是用户拥有的知识资产

- 每个分析结果必须生成标准 Markdown。
- Markdown 不依赖 Obsidian 专属语法。
- 用户可以把目录放进 Obsidian Vault、同步盘或其他自动化目录。
- 项目不开发 Google Docs、飞书、腾讯文档 API 接入。
- 用户自行处理在线文档同步。
- SQLite 是任务状态和搜索索引。
- SQLite 丢失后必须能扫描 Markdown 重建历史索引。
- 已完成报告不能只存在数据库里。

6. 不预测工期

所有开发计划只使用：

阶段
→ 前置依赖
→ 验收标准

禁止输出天数、周数、人日或完成日期预测。

# 三、明确的非目标

首版不包含：

- 远程服务器
- SaaS
- 用户注册
- 登录
- 积分
- 支付
- 管理后台
- 多租户
- MarketRadar
- SellerSprite
- Google Docs API
- 飞书 API
- 腾讯文档 API
- 在线知识库双向同步
- 云同步
- 远程 MCP
- 自动 git push
- 自动部署

# 四、技术栈

后端和本地运行时：

- Python 3.11
- FastAPI
- Uvicorn
- Pydantic Settings
- SQLAlchemy Async
- SQLite + aiosqlite
- SQLite FTS5
- MCP Python SDK
- httpx
- yt-dlp
- keyring
- platformdirs
- structlog
- tenacity

可选 ASR：

- faster-whisper
- CTranslate2
- FFmpeg

前端：

- React
- Vite
- React Router
- Tailwind CSS 4

工具链：

- uv
- pytest
- pytest-asyncio
- Ruff
- mypy
- npm
- Vitest
- ESLint
- PyInstaller onedir
- GitHub Actions

如果依赖包的当前名称或用法不确定，先查官方文档，不要凭记忆锁死版本。

# 五、标准目录结构

创建：

.
├── .agents/
│   ├── CHANGELOG.md
│   ├── experience.md
│   ├── feature_list.json
│   └── progress.md
├── .github/
│   ├── ISSUE_TEMPLATE/
│   └── workflows/
├── docs/
│   ├── adr/
│   ├── CLIENT_COMPATIBILITY.md
│   ├── CONFIGURATION.md
│   ├── JOB_STATE_MACHINE.md
│   ├── MARKDOWN_SCHEMA.md
│   ├── MCP_TOOLS.md
│   ├── PRD.md
│   ├── SYSTEM_ARCHITECTURE.md
│   ├── SYSTEM_BOUNDARIES.md
│   ├── TEST_STRATEGY.md
│   ├── WEBUI_SPEC.md
│   └── adr/0001-local-engine-and-stdio-bridge.md
├── src/
│   └── evoblue_video_hub/
│       ├── __init__.py
│       ├── application/
│       ├── clients/
│       ├── config/
│       ├── jobs/
│       ├── llm/
│       ├── mcp/
│       ├── platforms/
│       ├── reports/
│       ├── runtime/
│       ├── storage/
│       ├── transcript/
│       └── web/
├── frontend/
├── skills/
│   ├── codex/
│   └── workbuddy/
├── tests/
│   ├── contract/
│   ├── integration/
│   ├── unit/
│   └── evals/
├── AGENTS.md
├── init.sh
├── pyproject.toml
├── README.md
├── LICENSE
├── SECURITY.md
├── PRIVACY.md
├── CONTRIBUTING.md
├── THIRD_PARTY_NOTICES.md
└── .gitignore

不要创建：

- Codex.me
- 根目录 feature_list.json
- 根目录 progress.md
- 根目录 experience.md
- 根目录 CHANGELOG.md

Harness 状态文件全部放进 .agents/。

# 六、系统边界

采用单机模块化架构：

AI Client
→ STDIO MCP Bridge
→ Local Engine HTTP API on 127.0.0.1
→ Persistent Job Worker
→ Video Pipeline
→ SQLite + Markdown Store

Local Engine 负责：

- WebUI
- 本地 HTTP API
- 设置
- Credential Store
- SQLite
- Job Worker
- 视频 Pipeline
- 报告生成
- 搜索索引
- 客户端配置

STDIO MCP Bridge 负责：

- MCP 初始化
- 工具声明
- 参数验证
- 调用 Local Engine
- 将 Local Engine 结果转换成 MCP 结果

STDIO MCP Bridge 禁止：

- 直接访问数据库
- 直接运行 yt-dlp
- 直接运行 FFmpeg
- 直接调用 LLM
- 自己维护任务状态
- 在 stdout 打日志

MCP 使用 stdout 传协议，所有日志必须写 stderr 或日志文件。

# 七、Local Engine 生命周期

Local Engine：

- 仅绑定 127.0.0.1
- 使用固定可配置端口，默认值记录在架构文档
- 安装后可随系统启动
- 如果没有运行，MCP Bridge 可以安全地请求启动
- 只能存在一个 Worker Owner
- 多个 MCP Bridge 连接时共享同一个 Engine
- Engine 重启后恢复 queued/running/retry_wait 任务
- 不依赖仅存在内存里的 asyncio.create_task 作为唯一执行保证

初始化阶段只写清生命周期合同，不需要完整实现后台常驻机制。

# 八、MCP 工具合同

在 docs/MCP_TOOLS.md 中定义以下七个工具。

1. submit_video_analysis

输入：

{
  "url": "string",
  "mode": "auto | standard | unboxing",
  "asr": "auto | disabled | required",
  "language": "string | null"
}

立即返回：

{
  "job_id": "string",
  "status": "queued",
  "next_action": "get_analysis_status"
}

2. get_analysis_status

输入 job_id。

返回：

- status
- progress
- stage
- message
- retryable
- error_code
- error_detail

3. get_analysis_report

输入：

- job_id
- section: summary | outline | marketing | comments | metadata | transcript | full

返回：

- Markdown 文本或指定片段
- Markdown 文件绝对路径
- 报告 Schema 版本

必须避免默认返回过长字幕。

4. list_analysis_jobs

支持：

- limit
- offset
- status
- platform
- query

5. search_analysis_history

搜索：

- 标题
- 作者
- URL
- 标签
- 摘要
- 字幕

6. cancel_analysis

取消未完成任务。

7. diagnose_environment

检查：

- Local Engine
- 数据库
- 报告目录
- FFmpeg
- yt-dlp
- LLM 配置
- API 连通性
- CTranslate2
- GPU
- Whisper 模型
- Cookie 浏览器
- 磁盘空间

诊断结果必须脱敏。

所有工具都要定义：

- JSON Schema
- 成功返回
- 错误返回
- 幂等性
- 是否只读
- 超时行为
- 客户端兼容注意事项

# 九、Job 状态机

在 docs/JOB_STATE_MACHINE.md 中固定：

queued
→ fetching_metadata
→ fetching_subtitles
→ downloading_audio
→ transcribing
→ cleaning_transcript
→ chunking
→ summarizing_chunks
→ generating_report
→ indexing
→ completed

终止状态：

- completed
- failed
- cancelled

恢复状态：

- retry_wait

规则：

- 每个阶段幂等
- 状态变化先持久化
- 任务失败有稳定 error_code
- 重试次数有上限
- 重启后 running 任务不能永久卡住
- 同一个 URL + 模式 +配置指纹可复用近期任务
- cancel 必须检查当前阶段
- 临时文件与最终报告分离
- 报告写入采用临时文件 + 原子替换

# 十、Markdown Schema

在 docs/MARKDOWN_SCHEMA.md 定义：

---
schema_version: 1
analysis_id: ""
source_url: ""
platform: ""
video_id: ""
title: ""
author: ""
published_at: null
analyzed_at: ""
summary_mode: ""
language: ""
tags: []
---

正文标准章节：

# 标题
## 核心摘要
## 核心收获
## 时间轴大纲
## 内容分析
## 评论风向
## 视频信息
## 原始链接
## 完整字幕（可选）

规则：

- UTF-8
- 标准 Markdown
- YAML Frontmatter
- 图片相对路径
- 文件名清理 Windows 非法字符
- 用户可配置文件名模板
- 默认不覆盖用户修改过的 Markdown
- SQLite 索引可从 Markdown 重建
- Schema 版本升级必须向后兼容或提供迁移

# 十一、WebUI 页面合同

在 docs/WEBUI_SPEC.md 定义：

1. 首次设置向导
2. 历史记录首页
3. 新建分析
4. 任务进度
5. 报告详情
6. 全文搜索
7. LLM 设置
8. ASR 设置
9. Markdown 知识库设置
10. MCP 客户端设置
11. 隐私与存储
12. 系统诊断
13. 日志和错误报告

首屏必须让普通用户理解，不暴露内部工程术语。

MCP 客户端页面应显示：

- 是否安装
- 是否已配置
- MCP 是否能握手
- Engine 是否在线
- 安装配置
- 测试连接
- 移除配置
- 恢复备份

# 十二、客户端配置原则

Codex：

- 支持 ~/.codex/config.toml
- 使用 TOML AST 或安全解析方式修改
- 不覆盖其他 mcp_servers
- 修改前备份
- 配置后真实握手验证

Claude Desktop：

- Windows 配置位于 %APPDATA%\Claude\claude_desktop_config.json
- JSON 结构化合并
- 不覆盖其他 mcpServers
- 修改前备份
- 配置后真实握手验证

DeepSeek Harness：

- 使用官方 MCP Client 插件
- STDIO Transport
- 配置应作为独立适配器
- DeepSeek Harness 仍可能变化，核心代码禁止依赖它

WorkBuddy：

- 输出或安装本地 MCP 配置
- 提供单独 Skill
- WorkBuddy 适配不能进入视频核心
- 如果客户端内部配置格式无法可靠自动修改，WebUI 应生成可复制配置并明确提示，不得猜测路径或静默宣称成功

# 十三、安全合同

SECURITY.md 和 SYSTEM_BOUNDARIES.md 必须包含：

- 只绑定 127.0.0.1
- 禁止默认监听 0.0.0.0
- 防止恶意网页调用 localhost
- 严格 CORS
- SameSite Cookie
- 本机访问 Token
- API Key 存系统凭据库
- 日志脱敏
- URL 白名单和协议校验
- subprocess 使用参数数组，禁止 shell=True
- 输出目录白名单
- 下载大小限制
- 视频长度限制
- 并发限制
- 磁盘空间预检查
- 临时文件治理
- Cookie 不进入日志
- 报告中不泄露本机路径之外的敏感信息
- 遥测默认关闭
- 高风险操作需要用户确认

# 十四、从旧 EvoBlue 可以迁移的候选能力

只在文档中登记，初始化阶段不要复制代码：

- platform_detector
- ytdlp
- subtitle
- audio
- whisper
- transcript_cleaner
- chunking
- LLM client
- video prompts
- coverage checker

明确禁止整体复制：

- FastAPI 用户 API
- 用户认证
- 积分
- 支付
- Admin
- MarketRadar
- SellerSprite
- Share 页面
- 旧数据库模型
- 与线上域名绑定的逻辑

迁移原则：

- 先写新合同和测试
- 再理解旧实现
- 最后按新边界重写或迁移
- 不让旧架构决定新架构

# 十五、必须创建的 Harness 文件

严格按 project-init Skill 创建：

- AGENTS.md
- init.sh
- .agents/feature_list.json
- .agents/progress.md
- .agents/CHANGELOG.md
- .agents/experience.md

AGENTS.md 必须记录：

- Owner Context
- 项目目的
- 当前阶段
- 产品范围
- 技术栈
- 架构决策
- 环境命令
- Harness 文件
- 工作规则
- 不预测工期
- 不自动 git push
- 密钥不进代码
- 改完必须验证

init.sh：

- #!/bin/bash
- set -e
- 检查 Harness 文件
- 检查 Python、uv、Node、npm、Git
- 只做只读检查
- 不自动安装依赖
- 不启动服务
- 不部署
- 打印开工和收工流程

# 十六、初始化代码骨架

初始化阶段只创建：

- Python 包目录和空模块
- 可导入的包
- 配置对象骨架
- Job 状态 Enum
- MCP Tool 输入输出 Pydantic Schema
- ReportDocument Pydantic Schema
- FastAPI app factory 骨架
- /api/health 最小端点
- 最小 pytest
- Ruff 和 mypy 配置
- 前端目录与最小 package.json
- 前端测试和 lint 配置骨架
- GitHub Actions 基础 CI

不要实现：

- yt-dlp
- Whisper
- LLM 调用
- 客户端配置写入
- 后台常驻
- 完整 WebUI
- 视频 Pipeline

# 十七、开发阶段

把下面阶段写入 docs/PRD.md 和 .agents/progress.md，但不要预测时间：

P0：Harness、边界、合同、最小骨架
P1：Local Engine、SQLite、Worker、WebUI 框架
P2：YouTube/Bilibili 字幕分析闭环
P3：历史记录、Markdown、FTS5 和索引重建
P4：STDIO MCP Bridge 和四客户端
P5：WebUI MCP 自动配置与真实握手
P6：Faster-Whisper、模型管理和恢复
P7：Windows 安装器和 GitHub Release
P8：公开测试和 Harness 加固

每个阶段必须写：

- 目标
- 前置依赖
- 功能范围
- 非目标
- 验收标准
- 风险
- 回滚方式

禁止写工期。

# 十八、初始化验证

完成后必须运行：

- bash init.sh
- Python 包导入
- JSON 合法性检查
- Ruff
- mypy
- pytest
- 前端 package.json 合法性检查
- 如果依赖未安装，明确区分“环境缺失”和“代码失败”

不要为了通过测试而注释掉错误。

十九、交付要求

完成后告诉我：

1. 创建了哪些文件。
2. 关键架构决策。
3. 实际运行了哪些验证。
4. 哪些验证通过。
5. 哪些因为环境缺失未运行。
6. 下一步唯一建议任务是什么。

不要 git push。
不要启动长期运行的后台服务。
不要修改旧 EvoBlue 项目。
不要给工期预测。